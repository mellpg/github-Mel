#include <stdio.h>
#include <stdlib.h>

/*
int main() {

    int cep;

    // Solicita ao usuário os dois primeiros dígitos do CEP
    printf("Digite os dois primeiros dígitos do CEP: ");
    scanf("%d", &cep);

    // Verifica a região com base nos primeiros dígitos do CEP
    if (cep >= 80 && cep <= 99) {
        printf("Região: Sul\n");
    }
    else if (cep >= 1 && cep <= 39) {
        printf("Região: Sudeste\n");
    }
    else if (cep >= 70 && cep <= 79) {
        printf("Região: Centro-Oeste\n");
    }
    else if (cep >= 40 && cep <= 69) {
        printf("Região: Nordeste\n");
    }
    else if (cep >= 68 && cep <= 69) {
        printf("Região: Norte\n");
    }
    else {
        printf("Região: Importado\n");
    }

    return 0;
}
*/
/*
int main()
//Maçãs custam R$ 0,30 se forem compradas menos que uma dúzia e R$0,25 se forem
//compradas uma dúzia ou mais. Escreva um programa em pseudocódigo que leia o número
//de maçãs compradas, calcule e escreva na tela o valor total da compra.


{  int num;
   float valor;
   printf("\nDigite a quantidade:");
   scanf("%d", &num);
   if(num<12){
    valor = num*0.30;
   }
   if(num>12){
    valor = num*0.25;
   }
   printf("\nValor total da compra: %.2f $", valor);
   return 0;
}
*/
