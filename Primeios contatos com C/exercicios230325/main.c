#include <stdio.h>
#include <stdlib.h>
// exercicios
// kg para grama
/*
int main()
{  int kg,g,valor;
    printf("\tDigite um valor em KG:\n");
    scanf("%d", &kg);
    printf("\n\tValor: %d Kg", kg);
    g = kg*1000;
    printf("\n\tValor convertido: %d gramas", g);
    return 0;
}
*/
/*
int main()
// quadrado de um numero
{ int numero, quadrado;
  printf("\n\tDigite um numero:");
  scanf("%d", &numero);
  quadrado = numero*numero;
  printf("\n\tO quadrado do numero: %d", quadrado);
  return 0;
}
*/
/*
int main()
//Faça um algoritmo para ler um valor correspondente a um lado de um quadrado e mostrar a area
{ int l, area;
  printf("\n\tDigite um lado do quadrado:");
  scanf("%d", &l);
  area = l*l;
  printf("\n\tArea do quadrado: %d", area);
  return 0;
}
*/
/*
int main()
//) Faça um algoritmo para ler um valor em polegadas e escrever o equivalente em
//centímetros. OBS:1 polegada = 2.54 centímetros.
{  int polegadas,cm;     //Para converter polegadas em centímetros, basta multiplicar o número de polegadas por 2,54, que é o fator de conversão.
   printf("\n\tDigite um valor em polegadas:");
   scanf("%d", &polegadas);
   cm = polegadas*2.54;
   printf("\n\tValor em cm: %d cm", cm);
   return 0;
}
*/
/*
int main()
//Faça um algoritmo para ler base e altura de um triângulo e informar a área.
{ int b,a,area;
printf("\n\tDigite a base e altura de um triangulo:");
scanf("%d%d", &b,&a);
area = (b*a)/2;
printf("\n\tArea do triangulo: %d", area);
return 0;
}
*/
/*
int main()
//Faça um algoritmo para ler um valor real e apresentar este valor com desconto de
//10%.
{
float n,desconto;
printf("\n\tDigite um valor:");
scanf("%f", &n);
desconto = n-(n*10/100);
printf("\n\tValor com desconto: %.2f $", desconto);
return 0;
}
*/

